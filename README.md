# CampaignForPartners2
a simple web application that allows a user to create ad campaigns.
Steps:
1. npm install
2. MongoDB configuration：
use campaignforpartners
db.createCollection("adcampaign")

